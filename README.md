ข้อ 1: สร้างตัวแปรเก็บข้อมูลส่วนตัว แล้วใช้ Template Literal แสดงผล
ข้อ 2: เรียนรู้ชนิดข้อมูล เช่น string, number, boolean และใช้ typeof ตรวจชนิดข้อมูล
ข้อ 3: นำคะแนนแต่ละส่วนมาคำนวณ หาคะแนนรวม เปอร์เซ็นต์ และคะแนนที่ขาด
ข้อ 4: ใช้ if / else if ตรวจคะแนนเพื่อกำหนดเกรด พร้อมตรวจคะแนนที่ไม่ถูกต้อง
ข้อ 5: ใช้ switch เลือกเมนูอาหาร หาราคา และคำนวณตามขนาดกับจำนวน
ข้อ 6: ใช้ if ตรวจสอบการเข้าสู่ระบบ โดยตรวจ รหัสผ่าน → สถานะบัญชี → อายุ → สิทธิ์ ตามลำดับ